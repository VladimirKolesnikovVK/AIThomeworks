package de.ait.klassenarbeiten;

public class Ticket {
    /*public void bookTicket(String movieTitle, int numberOfTickets, Movie[] moviesSchedule) throws SoldOutException {
        for (Movie movie : moviesSchedule) {
            if (movie.getTitle().equals(movieTitle)) {
                int availableSeats = calculateAvailableSeats();
                if (availableSeats >= numberOfTickets) {
                    System.out.println("Билеты на фильм " + movieTitle + " успешно забронированы.");
                    return;
                } else {
                    throw new SoldOutException("Недостаточно свободных мест для бронирования билетов.");
                }
            }
        }
        System.out.println("Фильм с названием " + movieTitle + " не найден в расписании.");
    }*/
}
